include: base.yaml
dataset_type: nerflab
image_dir: null
down_sample_factor: 4
hold_for_test: 8
noise_std: 1e0
use_pose_depth: false  # only enable when training some objects like Lego or forward facing scene
near: null
far: null
no_undistort: false  # undistort via colmap is recommended
